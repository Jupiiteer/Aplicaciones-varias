let numUno = Number(prompt('Ingrese el primer valor', 0));
let numDos = Number(prompt('Ingrese el segundo valor', 0));
let resultado = Number(numUno + numDos);
document.write(`${numUno} + ${numDos} = ${resultado}`);
